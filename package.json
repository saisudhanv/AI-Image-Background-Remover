{
  "name": "ai-image-background-remover-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd client && npm ci && npm run build && rm -rf ../.build || true && mkdir -p ../.build && cp -r dist/* ../.build/",
    "start": "echo \"This repository is a monorepo. Start the specific app from its folder.\" && exit 0"
  }
}
